import { Inter, Raleway, Roboto_Mono } from 'next/font/google' // Import font functions from 'next/font/google'

// Define and export the 'inter' font with specified options
export const inter = Inter({
  subsets: ['latin'], // Define subset of characters (Latin)
  display: 'swap' // Specify font display behavior
})

// Define and export the 'raleway' font with specified options
export const raleway = Raleway({
  subsets: ['latin'], // Define subset of characters (Latin)
  display: 'swap' // Specify font display behavior
})

// Define and export the 'roboto_mono' font with specified options
export const roboto_mono = Roboto_Mono({
  subsets: ['latin'], // Define subset of characters (Latin)
  display: 'swap' // Specify font display behavior
})
